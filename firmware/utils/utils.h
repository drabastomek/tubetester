#ifndef UTILS_H
#define UTILS_H

#include <stdint.h>

uint8_t az_index(uint8_t c);

void zersrk(void);                 /* zeruj S, R, K */
uint16_t liczug1(uint16_t pug1);   /* przelicz Ug1 */

#endif